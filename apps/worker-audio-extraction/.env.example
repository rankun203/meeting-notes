# Generate a secret: openssl rand -hex 32
# Set the same value as LOCAL_WORKER_API_TOKEN on the server.
WORKER_API_TOKEN=
WORKER_BIND_ADDRESS=127.0.0.1
WORKER_PORT=8000
# For gated speaker diarization models; accept their access terms first.
HF_TOKEN=
# Optional tuning (leave unset to use the selected CPU/GPU defaults).
# WHISPER_MODEL_SIZE=small
# WHISPER_BATCH_SIZE=1
# OMP_NUM_THREADS=4
