PAYLOAD_SECRET=replace-with-at-least-32-random-characters
SERVER_URL=http://localhost:3000
DATABASE_ADAPTER=sqlite
DATABASE_URI=file:./data/gday.db
# For postgres: DATABASE_ADAPTER=postgres and DATABASE_URI=postgresql://...

# Docker host port/bind address and persistent volume. These affect Compose only.
SERVER_PORT=3000
SERVER_BIND_ADDRESS=127.0.0.1
SERVER_DATA_VOLUME=gday-meetings-data

# Optional: server-owned RunPod transcription (configure both to enable execution).
# RUNPOD_ENDPOINT_URL=https://api.runpod.ai/v2/your-endpoint-id
# RUNPOD_API_KEY=your-runpod-api-key

# Optional linked external OIDC sign-in. The server remains the source of users/roles.
# Configure all three, then link an existing account from /account.
# OIDC_UPSTREAM_ISSUER=https://identity.example.com
# OIDC_UPSTREAM_CLIENT_ID=gday-meetings
# OIDC_UPSTREAM_CLIENT_SECRET=your-provider-client-secret
# Optional auth storage override: local path/file: URI for SQLite, Postgres URI for postgres.
# AUTH_DATABASE_URI=

# Optional lower upload limit; maximum 500 MB (500,000,000 bytes).
MAX_UPLOAD_BYTES=500000000

# Optional local standalone worker instead of RunPod (machine auth only).
# TRANSCRIPTION_PROVIDER=local
# LOCAL_WORKER_URL=https://worker.example.com
# LOCAL_WORKER_API_TOKEN=match-the-worker-WORKER_API_TOKEN
# Optional worker-facing server origin if SERVER_URL is not reachable from it.
# Browser/SSO continue using SERVER_URL. No shared Docker network is assumed.
# SERVER_INTERNAL_URL=https://meetings.internal.example.com
